import 'package:flutter/material.dart';

class Buddyfullinfo
    extends
        StatelessWidget {
  const Buddyfullinfo({
    super.key,
  });

  @override
  Widget build(
    BuildContext
    context,
  ) {
    return Card();
  }
}
